<center><h1>List of books</h1>
<br>

<br>
<table border=1>
  <tr>
    <th>BOOK NAME</th>
    <th>AUTHOR</th>
    <th>DESCRIPTION</th>
   <th>BORROWED BY</th>
 <th>select student name</th>
<th>click to assign</th>
   <th>BORROWED ON</th>
   <th>RETURN DATE</th>
<th>DAYS LEFT</th>
    <th colspan="20"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= truncate(article.discp, :length => 20)%></td>

      <td>&nbsp<t> </t><%= link_to article.articlestud.title, articlestud_path(article.articlestud) if article.articlestud %></td>
      	<%= form_tag(assign_student_article_path(article), method: "get") do %>
		<td><%= select("article", "articlestud_id", Article.show_full_list,{ include_blank: true })%>
      		<td>&nbsp<t> </t><%= submit_tag('Assign')%></td>
    <td><%= article.borrow_date %></td>
    <td><%= article.return_date %></td>

  <td><%= article.remaining_days %></td>
    <td>&nbsp<t> </t><%= link_to 'Edit', edit_article_path(article) %></td>
    <td>&nbsp<t> </t><%= link_to 'Show', article_path(article) %></td>
        <% end %>
      
    </tr>
  <% end %>
</table>
<br>
<%= link_to 'New book', new_article_path %>
<br>
<br>
<td><%= link_to 'Home', welcome_index_path %></td>
</center>